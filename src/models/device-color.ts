export class DeviceColor {
    name: string;
    image: string;
    color: string;

    constructor(name: string, image: string, color: string) {
        this.name = name;
        this.image = image;
        this.color = color;
    }
}